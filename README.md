# llm_quant_estimator
WIP: recommended llm quantization estimator

## Usage

```bash
python -m llm_quant_estimator.llm_quant_estimator --model_id <model_id> --context <context> --batch <batch>
```

## Example

```bash
python -m llm_quant_estimator.llm_quant_estimator --model_id openai/gpt-oss-20b
```
